// Add TypeScript declarations for custom window properties
interface Window {
  FIREBASE_USE_EMULATORS?: boolean;
}
// Add TypeScript declarations for custom window properties
interface Window {
  FIREBASE_USE_EMULATORS?: boolean;
}
